export enum PAGES {
  HOME = '/',
  LOGIN = '/login',
  REGISTER = '/register',
  HOME_AUTH = '/home',
}

export enum NEED_AUTH {
  HOME_AUTH = '/home',
}

export enum NO_AUTH {
  HOME = '/',
  LOGIN = '/login',
  REGISTER = '/register',
}
